import styled from '@emotion/styled';
import { CardContent } from '@mui/material';

const ContentWithMargin = styled(CardContent)`
  cursor: pointer;
  margin-bottom: 50px;
`;

export default ContentWithMargin;
